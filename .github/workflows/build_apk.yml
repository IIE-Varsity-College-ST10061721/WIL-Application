# .github/workflows/debug_directory.yml
name: Debug Directory Structure

on:
  push:
    branches:
      - main
      - Dillon
      - Luke
      - Aphiwe  # Specify all relevant branches for building

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # List the root directory contents to understand the actual structure
      - name: List root directory contents
        run: ls -laR  # List all files and subdirectories recursively
